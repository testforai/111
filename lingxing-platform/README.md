# 领星 API ETL 与全接口控制台

独立的单公司多店铺系统。管理员可在网页配置领星/飞书凭据、补充官方接口、测试并发布接口，创建ETL任务并查看审计日志。

## Linux 启动

1. 复制 `.env.example` 为 `.env`，设置强随机密钥与初始管理员密码。
2. 运行 `docker compose up --build -d`。
3. 打开 `http://服务器地址`。

所有源码、构建和验证必须在 Linux 完成。